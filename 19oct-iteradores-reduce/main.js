const grandes =[1, 2, 3, 4];

const initialvalue = 0;
const reduceFn = (acc, cur, i, arr) =>{
    return acc +cur
}
const sumwithInitial = arrv1.reduce(reduceEn. initialvalue);
//const sumWinhInitial = array1.reduce (reduceFn);

console.log (sumwithInitial);
// expected output: 10

